## 1. Public foundation

- [x] 1.1 Add public guidance, Apache-2.0 attribution, dependency notices, and neutral assets.
- [x] 1.2 Define portable API-v1 models, error/progress results, adapter, executor, and direct client.
- [x] 1.3 Add the pinned Lua host build and restricted schema-v1 loader.
- [x] 1.4 Generate deterministic configuration, request, mapping, asset, and factory files.
- [x] 1.5 Add CMake product integration and native host presets.

## 2. Neutral demo

- [x] 2.1 Add the neutral product package and local example values.
- [x] 2.2 Add the C++17 SQLite server, migrations, seed, credential CLI, recognition write, and Docker image.
- [x] 2.3 Add host API, generator, direct client, and server tests.

## 3. Runtime extraction

- [x] 3.1 Port shared dashboard state, application flow, bridge protocol, and bounded avatar policy.
- [x] 3.2 Port Nintendo 3DS HTTP, credential, camera, graphics, audio, and shutdown lifecycles.
- [x] 3.3 Complete generated JSON response codecs for canonical dashboard models and recognition.
- [x] 3.4 Build generated bridge and Nintendo 3DS products without Lua runtime inputs.
- [x] 3.5 Replace the temporary generic renderer with the established bitmap-font session views, touch controls, dashboard hierarchy, stereoscopic scenes, and resource lifecycles from the pre-extraction application.
- [x] 3.6 Move every brand-bearing color, copy string, endpoint, launcher icon, GUI image, and diorama mark behind product-generated inputs while preserving the established layout and interaction model.
- [x] 3.7 Restore the browser pairing-status page as a shared bridge surface with a visible countdown, CSRF-protected manual offer rotation, automatic rotation after timeout, and reusable process lifecycle; retain a concise styled console status and call both QR and manual entry first-class pairing methods.
- [x] 3.8 Remove the duplicated generic footer legend and place input hints in the relevant controls, including `Scan QR (X)` and the manual pairing action.

## 4. Verification

- [x] 4.1 Run format, static analysis, host tests, clean deterministic generation, and strict OpenSpec validation.
- [x] 4.2 Build and smoke the container with persistence and reset scenarios.
- [ ] 4.3 Verify presentation parity, login, dashboard, recognition, expiry, recovery, audio, and exit in Azahar.
- [ ] 4.4 Complete textual, history, dependency, binary provenance, and visual-asset publication audits.
